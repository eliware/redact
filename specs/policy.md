# Policy

Redaction policy determines which structured keys and text patterns are
sensitive. Default policies must be conservative, case-insensitive for keys,
customizable by callers, and applied without mutating input values.
