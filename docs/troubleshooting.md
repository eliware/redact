# Troubleshooting

[Back to the project README](../README.md)

Run `npm test`, `npm run lint`, `npm run typecheck`, and `npm run pack` from
the repository root. If a reproducible issue remains, report it through the
repository issue tracker without including credentials or other secrets.
