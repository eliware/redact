version: 1
on:
  push:
    deployments:
      - target: dev
        cwd: /opt/redact
        commands:
          - git pull --ff-only
          - npm ci
          - npm test
          - npm audit --omit=dev --audit-level=moderate
          - npm pack --dry-run
